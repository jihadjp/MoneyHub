<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Transparent center -->
    <stroke
        android:color="@color/bg"
        android:width="2dp" />
    <corners android:radius="16dp" />
</shape>
