<?xml version="1.0" encoding="utf-8"?>
<!-- item_note_image.xml – thumbnail tile in the horizontal image strip -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="88dp"
    android:layout_height="88dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginEnd="8dp">

    <!-- Thumbnail (rounded corners via clip) -->
    <ImageView
        android:id="@+id/noteImageThumb"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:background="@drawable/bg_image_thumb"
        android:contentDescription="Attached image" />

    <!-- Remove button -->
    <ImageButton
        android:id="@+id/removeImageButton"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="top|end"
        android:layout_margin="4dp"
        android:background="@drawable/bg_remove_badge"
        android:contentDescription="Remove image"
        android:padding="3dp"
        android:src="@drawable/ic_close"
        app:tint="#FFFFFF" />

</FrameLayout>
